class Question {
  // Abstract class
  late String questionText;
  late bool questionAnswer;
  Question(this.questionText,
      this.questionAnswer); // this is similar to the constructor in java
  // Question(String q, bool a) {
  //   questionText = q;
  //   questionAnswer = a;
  // }
}
